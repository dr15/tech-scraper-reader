name: Scrape
on:
   schedule:
     - cron: "50 * * * *"

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - run: npm ci
      - run: npx nx run scraper:serve
